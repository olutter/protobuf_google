name: protobuf_google
description: Public interface definitions of Google APIs for Dart and gRPC.
version: 1.0.1
homepage: https://pwa.ir
repository: https://github.com/xclud/dart_protobuf_google/

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  fixnum: ^1.1.1
  protobuf: ^5.0.0

dev_dependencies:
  test: ^1.26.0
  lints: ^6.0.0
